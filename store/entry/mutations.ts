import { MutationTree } from 'vuex';
import { EntryState, Auth } from '@/store/types';

const mutations: MutationTree<EntryState> = {
  add: (state, auth: Auth) => {
  },
  remove: (state, auth: Auth) => {
  },
};

export default mutations;